# intentguard-agent
IntentGuard is an outcome enforcement layer for AI agents that ensures blockchain transactions execute within user-defined constraints or not at all. Agents express intent in natural language, compiled into on-chain checks. Transactions only execute if outcomes match intent, preventing MEV, slippage, and execution risk.
